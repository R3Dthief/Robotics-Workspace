# CAD

- `assemblies/` for top-level assemblies.
- `parts/` for individual components.
- `exports/stl` and `exports/step` for 3D printing and sharing.

# Naming Convention
    [Subsystem]_[Component]_[Version]_[State].[ext]
