## Repository Layout

- `projects/` — Individual projects (e.g. `RA1` for Robotic Arm v01).
- `shared/` — Hardware, code, and resources shared across projects.
- `templates/` — Project scaffolding used when creating new projects.
